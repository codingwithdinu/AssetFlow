<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <!-- Mail template for allocation notification -->
        <record id="mail_template_allocation" model="mail.template">
            <field name="name">AssetFlow: Asset Allocated</field>
            <field name="model_id" ref="model_assetflow_allocation"/>
            <field name="subject">Asset Allocated: {{ object.asset_id.name }}</field>
            <field name="body_html"><![CDATA[
<p>Dear {{ object.employee_id.name }},</p>
<p>Asset <strong>{{ object.asset_id.name }}</strong>
   (Tag: {{ object.asset_id.asset_tag }}) has been allocated to you.</p>
<p>Allocation Date: {{ object.allocation_date }}</p>
<p>Expected Return: {{ object.expected_return_date or 'Not specified' }}</p>
<p>Please acknowledge receipt. Contact AssetFlow team for any queries.</p>
            ]]></field>
            <field name="lang">{{ object.employee_id.user_id.lang }}</field>
        </record>
    </data>
</odoo>
